---
import {
  SignedIn,
  SignedOut,
  UserButton,
} from "@clerk/astro/components";

import "../styles/global.css";

interface Props {
  title?: string;
}

const { title = "PrimaDopu" } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | PrimaDopu</title>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100">
    <header
      class="bg-white shadow-sm border-b border-slate-200 sticky top-0 z-50"
    >
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center gap-8">
            <a href="/" class="flex items-center gap-2">
              <span
                class="text-2xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent"
              >
                PrimaDopu
              </span>
            </a>
            <SignedIn>
              <nav class="hidden md:flex items-center gap-1">
                <a
                  href="/generate"
                  class:list={[
                    "px-4 py-2 rounded-lg text-sm font-medium transition-colors",
                    currentPath === "/generate"
                      ? "bg-indigo-100 text-indigo-700"
                      : "text-slate-600 hover:bg-slate-100",
                  ]}
                >
                  🎨 Générer
                </a>
                <a
                  href="/library"
                  class:list={[
                    "px-4 py-2 rounded-lg text-sm font-medium transition-colors",
                    currentPath === "/library"
                      ? "bg-indigo-100 text-indigo-700"
                      : "text-slate-600 hover:bg-slate-100",
                  ]}
                >
                  📚 Bibliothèque
                </a>
                <a
                  href="/history"
                  class:list={[
                    "px-4 py-2 rounded-lg text-sm font-medium transition-colors",
                    currentPath === "/history"
                      ? "bg-indigo-100 text-indigo-700"
                      : "text-slate-600 hover:bg-slate-100",
                  ]}
                >
                  📜 Historique
                </a>
                <a
                  href="/profile"
                  class:list={[
                    "px-4 py-2 rounded-lg text-sm font-medium transition-colors",
                    currentPath === "/profile"
                      ? "bg-indigo-100 text-indigo-700"
                      : "text-slate-600 hover:bg-slate-100",
                  ]}
                >
                  👤 Profil
                </a>
              </nav>
            </SignedIn>
          </div>
          <div class="flex items-center gap-4">
            <SignedOut>
              <a
                href="/sign-in"
                class="px-4 py-2 bg-indigo-600 text-white rounded-lg text-sm font-medium hover:bg-indigo-700 transition-colors cursor-pointer"
              >
                Se connecter
              </a>
            </SignedOut>
            <SignedIn>
              <UserButton
                appearance={{
                  elements: {
                    avatarBox: "w-10 h-10",
                  },
                }}
              />
            </SignedIn>
          </div>
        </div>
        <!-- Mobile nav -->
        <SignedIn>
          <nav class="md:hidden flex items-center gap-1 pb-3 overflow-x-auto">
            <a
              href="/generate"
              class:list={[
                "px-3 py-1.5 rounded-lg text-xs font-medium transition-colors whitespace-nowrap",
                currentPath === "/generate"
                  ? "bg-indigo-100 text-indigo-700"
                  : "text-slate-600 hover:bg-slate-100",
              ]}
            >
              🎨 Générer
            </a>
            <a
              href="/library"
              class:list={[
                "px-3 py-1.5 rounded-lg text-xs font-medium transition-colors whitespace-nowrap",
                currentPath === "/library"
                  ? "bg-indigo-100 text-indigo-700"
                  : "text-slate-600 hover:bg-slate-100",
              ]}
            >
              📚 Bibliothèque
            </a>
            <a
              href="/history"
              class:list={[
                "px-3 py-1.5 rounded-lg text-xs font-medium transition-colors whitespace-nowrap",
                currentPath === "/history"
                  ? "bg-indigo-100 text-indigo-700"
                  : "text-slate-600 hover:bg-slate-100",
              ]}
            >
              📜 Historique
            </a>
            <a
              href="/profile"
              class:list={[
                "px-3 py-1.5 rounded-lg text-xs font-medium transition-colors whitespace-nowrap",
                currentPath === "/profile"
                  ? "bg-indigo-100 text-indigo-700"
                  : "text-slate-600 hover:bg-slate-100",
              ]}
            >
              👤 Profil
            </a>
          </nav>
        </SignedIn>
      </div>
    </header>
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <slot />
    </main>
  </body>
</html>
